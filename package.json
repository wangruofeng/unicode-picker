{
  "name": "unicode-symbol-picker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "check": "astro check",
    "test": "node --import tsx --test tests/**/*.test.ts",
    "data:download": "node scripts/download-unicode.mjs",
    "data:parse": "node scripts/parse-ucd.mjs",
    "data:index": "node scripts/build-search-index.mjs",
    "data:validate": "node scripts/validate-data.mjs",
    "data:build": "npm run data:download && npm run data:parse && npm run data:index && npm run data:validate"
  },
  "dependencies": {
    "astro": "^5.13.5"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.4",
    "@types/node": "^24.3.0",
    "tsx": "^4.20.3",
    "typescript": "^5.9.2"
  }
}
